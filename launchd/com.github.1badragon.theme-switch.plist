<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.github.1badragon.theme-switch</string>

	<!-- launchd does not expand ~ or $HOME inside ProgramArguments, and this
	     repo is deployed under whatever user checks it out, so the path is
	     resolved by a shell at launch instead of being baked in here. -->
	<key>ProgramArguments</key>
	<array>
		<string>/bin/sh</string>
		<string>-c</string>
		<string>exec "$HOME/.local/bin/theme-switch" auto --watch</string>
	</array>

	<key>RunAtLoad</key>
	<true/>

	<!-- The watcher never exits on purpose, so any exit is worth retrying;
	     KeepAlive restarts it regardless of status. ThrottleInterval is the
	     floor between attempts, so a failing binary cannot spin. -->
	<key>KeepAlive</key>
	<true/>
	<key>ThrottleInterval</key>
	<integer>5</integer>

	<!-- A LaunchAgent already runs only inside a GUI login session, which is
	     the launchd equivalent of the systemd unit's graphical-session.target
	     binding. Output goes to the unified log; see the README for the
	     log-show invocation that reads it back. -->
</dict>
</plist>
